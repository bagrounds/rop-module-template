{
  "name": "serve-function-module-template",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "start": "node bin/aFunction",
    "test": "_mocha",
    "predocument": "rm -r docs",
    "document": "jsdoc --destination docs -c conf/jsdoc-conf.json -r",
    "precoverage": "rm -r coverage",
    "coverage": "istanbul cover _mocha"
  },
  "dependencies": {
    "argparse": "^1.0.7",
    "type-check": "^0.3.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.3",
    "jsdoc": "^3.4.0",
    "mocha": "^2.4.5"
  }
}
